@model IEnumerable<BW_Admin.tbl_Account>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.acc_Password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.acc_Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.acc_Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.acc_Active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.acc_UserID }) |
            @Html.ActionLink("Details", "Details", new { id=item.acc_UserID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.acc_UserID })
        </td>
    </tr>
}

</table>
